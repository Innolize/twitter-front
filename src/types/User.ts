export type User = {
    createdAt?: string,
    email?: string,
    name: string,
    profilePicture: string | null,
    cover: string | null,
    surname: string,
    updatedAt?: string,
    _id: string
}