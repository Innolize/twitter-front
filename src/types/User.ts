export type User = {
    createdAt?: string,
    email?: string,
    name: string,
    bio: string,
    profilePicture: string | null,
    cover: string | null,
    surname: string,
    updatedAt?: string,
    _id: string
}